import 'package:cash_ui/cash_text.dart';
import 'package:flutter/widgets.dart';

class CashAssetScreen extends StatelessWidget {
  const CashAssetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: CashText("Assets"));
  }
}
